class BaseComponent {
    layer = 0;
    visible = true;

    /**
     * @param {number} deltaTime
     */
    update(deltaTime) {
        //
    };

    /**
     * @param {CanvasRenderingContext2D} context
     */
    render(context) {

    };
}